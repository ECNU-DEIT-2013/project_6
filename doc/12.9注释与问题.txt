clubsend页面（社团查看学生报名情况）
1）服务器从数据库中读出社团名称，并传入客户端clubsend页面的下拉菜单中
2）客户端将两个下拉菜单选择的数据写成sql语句传给服务器。
3）服务器执行sql语句，从数据库中读取需要的字段，以json格式传回。
问题：
1）clubsend页面需两次接受服务器传来的不同数据，但目前每次接收到的都是第一次传来的数据。
2）上传到github中master上的更新莫名消失...